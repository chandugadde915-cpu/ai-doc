# Leave unset for local dev - the Vite dev server proxies /api/* to the local backend
# (see vite.config.js). Set this in Vercel's project settings to your deployed backend's URL,
# e.g. https://ai-document-intelligence-backend.onrender.com
VITE_API_BASE_URL=
